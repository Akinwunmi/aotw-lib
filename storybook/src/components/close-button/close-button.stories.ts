import { Meta } from '@storybook/web-components';

import { CloseButtonTemplate } from './close-button.model';

export default {
  title: 'Components/Close Button (deprecated)'
} as Meta;

export const Default = CloseButtonTemplate.bind({});
